# Copy to .env.local (gitignored by default in Vite projects).
# Vite only exposes variables prefixed with VITE_ to the client bundle.

# Base URL of the Django/DRF backend.
# Leave unset for local development and the client falls back to this value.
VITE_API_BASE_URL=http://localhost:8000
