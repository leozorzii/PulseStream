/*
 * Placeholder shell. Routing and the real screens go here — see src/pages.
 * Kept deliberately bare so there is nothing to delete before building.
 */
export default function App() {
  return (
    <main className="min-h-screen p-8">
      <h1 className="text-2xl font-semibold">PulseStream</h1>
      <p className="mt-2 text-text-muted">Frontend scaffold — nothing built yet.</p>
    </main>
  )
}
