# hooks

Custom React hooks. Mostly data-fetching wrappers around `services/` —
`useSources()`, `useSentimentSummary(sourceId)` — so pages stay declarative
and loading/error handling is not copy-pasted across screens.
