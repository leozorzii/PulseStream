# lib

Framework-agnostic helpers: formatters, label maps, small pure functions.
Nothing here imports React.

Likely first resident: the map from the API's sentiment codes
(`POS` / `NEU` / `NEG`) to display labels and their token class names.
